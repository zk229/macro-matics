<html lang="en">

  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Macro-Matics</title>
    <link rel="icon" href="https://upload.wikimedia.org/wikipedia/commons/d/d8/Eo_circle_blue_letter-m.svg">
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300&display=swap" rel="stylesheet">
  </head>

  <body>
    <header>
      <h1>Macro-Matics</h1>

      <nav>
        <ul>
          <li>
            <a href="/">Home</a>
            {{#if loggedIn}}
              <a id="logout">Logout</a>
            {{else}}
              <a href="/login">Login</a>
            {{/if}}
          </li>
        </ul>
      </nav>
    </header>
    <main>
      <div>
        {{{body}}}
      </div>
      <div>
        {{#if loggedIn}}
          <a class="button" href="/add-workout">Add a Workout</a>
          <a class="button" href="/add-meal">Add a Meal</a>
          <a class="button" href="/view-stats">Today's Stats</a>
        {{/if}}
      </div>
    </main>
    {{#if loggedIn}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>